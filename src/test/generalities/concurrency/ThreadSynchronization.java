package generalities.concurrency;


/**
 * Object's Condition variable with wait(), notify(), notifyAll()
 * await() : Causes the current thread to wait until it is signaled or interrupted
 *  and the lock associated with this Condition is atomically released.
 *  Also when the thread returns it is guaranteed to hold this lock.
 */
public class ThreadSynchronization {


}
